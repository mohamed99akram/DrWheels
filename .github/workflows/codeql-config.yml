name: CodeQL Config Placeholder

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  noop:
    name: No-op
    runs-on: ubuntu-latest
    steps:
      - name: No-op
        run: echo "This file exists to avoid invalid workflow errors. Actual CodeQL config is in .github/codeql/codeql-config.yml"
