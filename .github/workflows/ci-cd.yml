name: CI/CD Pipeline

on:
  push:
    branches: [ master, develop, feature/* ]
  pull_request:
    branches: [ master, develop ]

env:
  NODE_VERSION: '18'
  DOCKER_REGISTRY: ${{ secrets.DOCKER_REGISTRY }}

# ============================================
# STAGE 1: BUILD & TEST
# ============================================
jobs:
  # Backend Tests
  backend-tests:
    name: Backend Tests
    runs-on: ubuntu-latest
    services:
      mongo:
        image: mongo:6
        ports:
          - 27017:27017
        options: >-
          --health-cmd "mongosh --eval 'db.adminCommand(\"ping\")'" 
          --health-interval 10s 
          --health-timeout 5s 
          --health-retries 5
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'
          cache-dependency-path: backend/package-lock.json
      
      - name: Install dependencies
        working-directory: ./backend
        run: npm ci || (rm -f package-lock.json && npm install)
      
      - name: Wait for MongoDB
        run: |
          echo "⏳ Waiting for MongoDB service container to be ready..."
          echo "📋 MongoDB service configuration:"
          echo "   - Image: mongo:6"
          echo "   - Port: 27017"
          echo "   - Health check: Enabled"
          sleep 10
          
          # Check MongoDB connection
          echo ""
          echo "🔍 Verifying MongoDB connection..."
          MAX_ATTEMPTS=10
          ATTEMPT=0
          
          while [ $ATTEMPT -lt $MAX_ATTEMPTS ]; do
            ATTEMPT=$((ATTEMPT + 1))
            echo "   Attempt $ATTEMPT/$MAX_ATTEMPTS: Testing connection..."
            
            # Try to connect and ping MongoDB
            if mongosh --host localhost:27017 --eval "db.adminCommand('ping')" --quiet 2>/dev/null; then
              echo ""
              echo "✅ MongoDB is CONNECTED and responding!"
              echo "   Connection details:"
              mongosh --host localhost:27017 --eval "db.adminCommand('ping')" --quiet
              echo "   Server version:"
              mongosh --host localhost:27017 --eval "db.version()" --quiet
              echo ""
              echo "✅ MongoDB service is ready for use"
              exit 0
            fi
            
            if [ $ATTEMPT -lt $MAX_ATTEMPTS ]; then
              echo "   ⏳ Not ready yet, waiting 2 seconds..."
              sleep 2
            fi
          done
          
          echo ""
          echo "⚠️  Warning: Could not verify MongoDB connection after $MAX_ATTEMPTS attempts"
          echo "💡 Note: Tests use mongodb-memory-server, so this may not be critical"
          echo "   However, if you need the MongoDB service, check the service container logs"
      
      - name: Run unit tests
        working-directory: ./backend
        run: npm run test:ci
        env:
          NODE_ENV: test
          JWT_SECRET: test-secret-key-for-jwt-tokens-in-ci
          JWT_EXPIRE: 7d
          # Don't set MONGODB_URI - tests use mongodb-memory-server
        continue-on-error: false
      
      - name: Upload coverage reports
        uses: codecov/codecov-action@v3
        with:
          file: ./backend/coverage/lcov.info
          flags: backend
          name: backend-coverage
          fail_ci_if_error: false

  # Frontend Tests
  frontend-tests:
    name: Frontend Tests
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json
      
      - name: Install dependencies
        working-directory: ./frontend
        run: npm ci || (rm -f package-lock.json && npm install)
      
      - name: Run ESLint
        working-directory: ./frontend
        run: npm run lint
        continue-on-error: true
      
      - name: Run unit tests
        working-directory: ./frontend
        run: npm run test:ci
        env:
          CI: true
          REACT_APP_API_URL: http://localhost:4000/api
          SKIP_PREFLIGHT_CHECK: true
        continue-on-error: false
      
      - name: Upload coverage reports
        uses: codecov/codecov-action@v3
        with:
          file: ./frontend/coverage/lcov.info
          flags: frontend
          name: frontend-coverage
          fail_ci_if_error: false

# ============================================
# STAGE 2: SECURITY SCANNING
# ============================================
  backend-security-scan:
    name: Backend Security Scan
    runs-on: ubuntu-latest
    permissions:
      actions: read
      contents: read
      security-events: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'
          cache-dependency-path: backend/package-lock.json
      
      - name: Install dependencies
        working-directory: ./backend
        run: npm ci || (rm -f package-lock.json && npm install)

      # Static Code Analysis
      - name: ESLint Security Scan
        working-directory: ./backend
        run: |
          npx eslint . \
            --config .eslintrc.security.js \
            --max-warnings=0 \
            --format json \
            --output-file eslint-report.json || true
      
      - name: Upload ESLint results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: backend-eslint-report
          path: backend/eslint-report.json

      - name: Initialize CodeQL
        uses: github/codeql-action/init@v3
        with:
          languages: javascript
          config-file: .github/codeql/codeql-config.yml
      
      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@v3
        with:
          upload: false
          output: codeql-results

      - name: Upload CodeQL results (SARIF)
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: backend-codeql-sarif
          path: codeql-results/**/*.sarif

      # Dependency Scanning
      - name: Dependency & License Scan (Anchore)
        uses: anchore/scan-action@v3
        with:
          path: ./backend
          fail-build: true
          severity-cutoff: high
      
      - name: Snyk Dependency & License Scan
        uses: snyk/actions/node@master
        env:
          SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
        with:
          args: --severity-threshold=high
        continue-on-error: true

      # Secrets Scanning
      - name: Secrets Scan (Gitleaks)
        uses: gitleaks/gitleaks-action@v2
        with:
          fail: true
          config-path: .github/gitleaks.toml
          verbose: true
          exit-code: 1
          commit: ${{ github.sha }}
          repository: ${{ github.repository }}
          branch: ${{ github.ref }}

      # IaC Security Scanning
      - name: Infrastructure as Code Security Scan (Checkov)
        uses: bridgecrewio/checkov-action@v8
        with:
          directory: ./terraform
          framework: terraform
          soft-fail: true
        continue-on-error: true

      # SBOM Generation
      - name: Generate Backend SBOM
        uses: aquasecurity/trivy-action@master
        with:
          scan-type: 'fs'
          scan-ref: './backend'
          format: 'cyclonedx'
          output: 'sbom-backend.xml'
        continue-on-error: true
      
      - name: Upload SBOM artifacts
        uses: actions/upload-artifact@v4
        with:
          name: backend-sbom
          path: sbom-backend.xml

  frontend-security-scan:
    name: Frontend Security Scan
    runs-on: ubuntu-latest
    permissions:
      actions: read
      contents: read
      security-events: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json
      
      - name: Install dependencies
        working-directory: ./frontend
        run: npm ci || (rm -f package-lock.json && npm install)

      # Static Code Analysis
      - name: ESLint Security Scan
        working-directory: ./frontend
        run: |
          npx eslint src \
            --config .eslintrc.json \
            --max-warnings=0 \
            --format json \
            --output-file eslint-report.json || true
      
      - name: Upload ESLint results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: frontend-eslint-report
          path: frontend/eslint-report.json

      - name: Initialize CodeQL
        uses: github/codeql-action/init@v3
        with:
          languages: javascript
          config-file: .github/codeql/codeql-config.yml
      
      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@v3
        with:
          upload: false
          output: codeql-results

      - name: Upload CodeQL results (SARIF)
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: frontend-codeql-sarif
          path: codeql-results/**/*.sarif

      # Dependency Scanning
      - name: Snyk Dependency & License Scan (Frontend)
        uses: snyk/actions/node@master
        env:
          SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
        with:
          args: --severity-threshold=high --file=frontend/package.json
        continue-on-error: true

      # SBOM Generation
      - name: Generate Frontend SBOM
        uses: aquasecurity/trivy-action@master
        with:
          scan-type: 'fs'
          scan-ref: './frontend'
          format: 'cyclonedx'
          output: 'sbom-frontend.xml'
        continue-on-error: true
      
      - name: Upload SBOM artifacts
        uses: actions/upload-artifact@v4
        with:
          name: frontend-sbom
          path: sbom-frontend.xml

# ============================================
# STAGE 3: BUILD DOCKER IMAGES
# ============================================
  build-images:
    name: Build Docker Images
    needs: [backend-tests, frontend-tests, backend-security-scan, frontend-security-scan]
    runs-on: ubuntu-latest
    if: github.event_name == 'push'
    outputs:
      backend-image: ${{ steps.image-tags.outputs.backend_image_ref }}
      frontend-image: ${{ steps.image-tags.outputs.frontend_image_ref }}
      image-digest-backend: ${{ steps.build-backend.outputs.digest }}
      image-digest-frontend: ${{ steps.build-frontend.outputs.digest }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      
      - name: Log in to Docker Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ secrets.DOCKER_REGISTRY }}
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
      
      # Backend Image
      - name: Extract backend metadata
        id: meta-backend
        uses: docker/metadata-action@v5
        with:
          images: ${{ secrets.DOCKER_REGISTRY }}/devsecopscourse/drwheels/backend
          tags: |
            type=ref,event=branch
            type=sha,prefix={{branch}}-
            type=raw,value=latest,enable={{is_default_branch}}
      
      - name: Build and push backend image
        id: build-backend
        uses: docker/build-push-action@v5
        with:
          context: ./backend
          push: true
          tags: ${{ steps.meta-backend.outputs.tags }}
          labels: ${{ steps.meta-backend.outputs.labels }}
          cache-from: type=registry,ref=${{ secrets.DOCKER_REGISTRY }}/devsecopscourse/drwheels/backend:buildcache
          cache-to: type=registry,ref=${{ secrets.DOCKER_REGISTRY }}/devsecopscourse/drwheels/backend:buildcache,mode=max
      
      # Frontend Image
      - name: Extract frontend metadata
        id: meta-frontend
        uses: docker/metadata-action@v5
        with:
          images: ${{ secrets.DOCKER_REGISTRY }}/devsecopscourse/drwheels/frontend
          tags: |
            type=ref,event=branch
            type=sha,prefix={{branch}}-
            type=raw,value=latest,enable={{is_default_branch}}

      - name: Select single image tags
        id: image-tags
        shell: bash
        run: |
          echo "backend_image_ref=$(echo '${{ steps.meta-backend.outputs.tags }}' | head -n 1)" >> $GITHUB_OUTPUT
          echo "frontend_image_ref=$(echo '${{ steps.meta-frontend.outputs.tags }}' | head -n 1)" >> $GITHUB_OUTPUT
      
      - name: Build and push frontend image
        id: build-frontend
        uses: docker/build-push-action@v5
        with:
          context: ./frontend
          push: true
          tags: ${{ steps.meta-frontend.outputs.tags }}
          labels: ${{ steps.meta-frontend.outputs.labels }}
          cache-from: type=registry,ref=${{ secrets.DOCKER_REGISTRY }}/devsecopscourse/drwheels/frontend:buildcache
          cache-to: type=registry,ref=${{ secrets.DOCKER_REGISTRY }}/devsecopscourse/drwheels/frontend:buildcache,mode=max

      # Image Vulnerability Scanning
      - name: Scan Backend Image (Trivy)
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: ${{ steps.image-tags.outputs.backend_image_ref }}
          format: 'sarif'
          output: 'trivy-backend.sarif'
          severity: 'CRITICAL,HIGH'
        continue-on-error: true
      
      - name: Upload Trivy Backend results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: trivy-backend-sarif
          path: trivy-backend.sarif
      
      - name: Scan Frontend Image (Trivy)
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: ${{ steps.image-tags.outputs.frontend_image_ref }}
          format: 'sarif'
          output: 'trivy-frontend.sarif'
          severity: 'CRITICAL,HIGH'
        continue-on-error: true
      
      - name: Upload Trivy Frontend results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: trivy-frontend-sarif
          path: trivy-frontend.sarif

# ============================================
# STAGE 4: DEPLOY TO DEVELOPMENT
# ============================================
  deploy-dev:
    name: Deploy to Development
    needs: build-images
    runs-on: ubuntu-latest
    environment:
      name: development
    if: github.ref == 'refs/heads/develop'
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      
      - name: Deploy to Development
        run: |
          echo "Deploying to development environment..."
          echo "Backend Image: ${{ needs.build-images.outputs.backend-image }}"
          echo "Frontend Image: ${{ needs.build-images.outputs.frontend-image }}"
          # Add your deployment commands here
          # Example: kubectl set image deployment/backend backend=${{ needs.build-images.outputs.backend-image }}

# ============================================
# STAGE 5: DEPLOY TO STAGING & DAST
# ============================================
  deploy-staging:
    name: Deploy to Staging
    needs: build-images
    runs-on: ubuntu-latest
    environment:
      name: staging
    if: github.ref == 'refs/heads/master' || github.ref == 'refs/heads/develop'
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Select image tags for staging deployment
        shell: bash
        run: |
          set -euo pipefail

          if [ -z "${DOCKER_REGISTRY:-}" ]; then
            echo "ERROR: DOCKER_REGISTRY env var is empty (check secrets.DOCKER_REGISTRY)"
            exit 1
          fi

          SHORT_SHA="${GITHUB_SHA::7}"
          BACKEND_IMAGE_REF="${DOCKER_REGISTRY}/devsecopscourse/drwheels/backend:${GITHUB_REF_NAME}-${SHORT_SHA}"
          FRONTEND_IMAGE_REF="${DOCKER_REGISTRY}/devsecopscourse/drwheels/frontend:${GITHUB_REF_NAME}-${SHORT_SHA}"

          if [ -z "$BACKEND_IMAGE_REF" ] || [ -z "$FRONTEND_IMAGE_REF" ]; then
            echo "ERROR: Computed image refs are empty"
            exit 1
          fi

          echo "Backend image ref (computed): $BACKEND_IMAGE_REF"
          echo "Frontend image ref (computed): $FRONTEND_IMAGE_REF"

          echo "BACKEND_IMAGE_REF=$BACKEND_IMAGE_REF" >> $GITHUB_ENV
          echo "FRONTEND_IMAGE_REF=$FRONTEND_IMAGE_REF" >> $GITHUB_ENV
      
      - name: Deploy to Staging (GitOps)
        run: |
          echo "Triggering GitOps deployment to staging..."
          echo "Backend Image: $BACKEND_IMAGE_REF"
          echo "Frontend Image: $FRONTEND_IMAGE_REF"
          
          # Update GitOps repository with new image tags
          git config --global user.name "GitHub Actions"
          git config --global user.email "actions@github.com"
          
          # Clone the GitOps repo
          git clone https://${{ secrets.WHEELOPS_TOKEN }}@github.com/mohamed99akram/wheelops.git
          cd wheelops
          
          # Update backend image tag
          if [ -f drwheels/backend-deployment.yaml ]; then
            sed -i -E "0,/^[[:space:]]*image:/s|^([[:space:]]*)image:.*|\\1image: $BACKEND_IMAGE_REF|" drwheels/backend-deployment.yaml
            echo "Updated backend deployment manifest:"
            grep -n "image:" -n drwheels/backend-deployment.yaml | head -n 20 || true
          else
            echo "ERROR: wheelops/drwheels/backend-deployment.yaml not found"
            exit 1
          fi
          
          # Update frontend image tag
          if [ -f drwheels/frontend-deployment.yaml ]; then
            sed -i -E "0,/^[[:space:]]*image:/s|^([[:space:]]*)image:.*|\\1image: $FRONTEND_IMAGE_REF|" drwheels/frontend-deployment.yaml
            echo "Updated frontend deployment manifest:"
            grep -n "image:" -n drwheels/frontend-deployment.yaml | head -n 20 || true
          else
            echo "ERROR: wheelops/drwheels/frontend-deployment.yaml not found"
            exit 1
          fi
          
          # Commit and push changes to trigger GitOps
          git add drwheels/backend-deployment.yaml drwheels/frontend-deployment.yaml

          if git diff --cached --quiet; then
            echo "No manifest changes detected after update; nothing to deploy."
            exit 0
          fi

          git commit -m "Update staging images to ${{ github.sha }}"
          git push
          
          echo "GitOps deployment triggered. Waiting for Azure to deploy..."

      - name: Setup kubectl
        uses: azure/setup-kubectl@v4

      - name: Monitor staging rollout (backend + frontend)
        shell: bash
        env:
          KUBECONFIG_B64: ${{ secrets.KUBECONFIG_B64 }}
          STAGING_NAMESPACE: ${{ secrets.STAGING_NAMESPACE }}
          BACKEND_DEPLOYMENT: ${{ secrets.STAGING_BACKEND_DEPLOYMENT }}
          FRONTEND_DEPLOYMENT: ${{ secrets.STAGING_FRONTEND_DEPLOYMENT }}
        run: |
          set -euo pipefail

          if [ -z "${KUBECONFIG_B64:-}" ]; then
            echo "KUBECONFIG_B64 not set; skipping kubectl rollout monitoring."
            exit 0
          fi

          if [ -z "${STAGING_NAMESPACE:-}" ]; then
            STAGING_NAMESPACE="default"
          fi
          if [ -z "${BACKEND_DEPLOYMENT:-}" ]; then
            BACKEND_DEPLOYMENT="backend"
          fi
          if [ -z "${FRONTEND_DEPLOYMENT:-}" ]; then
            FRONTEND_DEPLOYMENT="frontend"
          fi

          echo "Writing kubeconfig..."
          mkdir -p "$HOME/.kube"
          echo "$KUBECONFIG_B64" | base64 -d > "$HOME/.kube/config"
          chmod 600 "$HOME/.kube/config"

          echo "Cluster info (best-effort):"
          kubectl version --client=true
          kubectl cluster-info || true

          echo "Current images before rollout check:"
          kubectl -n "$STAGING_NAMESPACE" get deploy "$BACKEND_DEPLOYMENT" "$FRONTEND_DEPLOYMENT" -o=jsonpath='{range .items[*]}{.metadata.name}{"\t"}{range .spec.template.spec.containers[*]}{.image}{"\n"}{end}{end}' || true

          echo "Waiting for backend rollout: $STAGING_NAMESPACE/$BACKEND_DEPLOYMENT"
          kubectl -n "$STAGING_NAMESPACE" rollout status "deployment/$BACKEND_DEPLOYMENT" --timeout=10m

          echo "Waiting for frontend rollout: $STAGING_NAMESPACE/$FRONTEND_DEPLOYMENT"
          kubectl -n "$STAGING_NAMESPACE" rollout status "deployment/$FRONTEND_DEPLOYMENT" --timeout=10m

          echo "Pods summary:"
          kubectl -n "$STAGING_NAMESPACE" get pods -o wide

        continue-on-error: true

      - name: Dump staging pods/events (on rollout issues)
        if: always()
        shell: bash
        env:
          KUBECONFIG_B64: ${{ secrets.KUBECONFIG_B64 }}
          STAGING_NAMESPACE: ${{ secrets.STAGING_NAMESPACE }}
        run: |
          set -euo pipefail

          if [ -z "${KUBECONFIG_B64:-}" ]; then
            exit 0
          fi
          if [ -z "${STAGING_NAMESPACE:-}" ]; then
            STAGING_NAMESPACE="default"
          fi

          mkdir -p "$HOME/.kube"
          echo "$KUBECONFIG_B64" | base64 -d > "$HOME/.kube/config"
          chmod 600 "$HOME/.kube/config"

          echo "Recent events:"
          kubectl -n "$STAGING_NAMESPACE" get events --sort-by=.lastTimestamp | tail -n 50 || true

          echo "Pods (describe failing ones):"
          kubectl -n "$STAGING_NAMESPACE" get pods -o wide || true
          for p in $(kubectl -n "$STAGING_NAMESPACE" get pods --no-headers 2>/dev/null | awk '{print $1}'); do
            PHASE=$(kubectl -n "$STAGING_NAMESPACE" get pod "$p" -o jsonpath='{.status.phase}' 2>/dev/null || echo "")
            READY=$(kubectl -n "$STAGING_NAMESPACE" get pod "$p" -o jsonpath='{.status.containerStatuses[0].ready}' 2>/dev/null || echo "")
            if [ "$PHASE" != "Running" ] || [ "$READY" != "true" ]; then
              echo "--- kubectl describe pod/$p ---"
              kubectl -n "$STAGING_NAMESPACE" describe pod "$p" || true
              echo "--- kubectl logs pod/$p (tail) ---"
              kubectl -n "$STAGING_NAMESPACE" logs "$p" --all-containers --tail=200 || true
            fi
          done
        continue-on-error: true
      
      - name: Wait for staging environment to be ready
        env:
          STAGING_URL: ${{ secrets.STAGING_ENVIRONMENT_URL || 'https://staging.drwheels.com' }}
        run: |
          echo "Checking if staging environment is ready..."
          echo "Staging URL: $STAGING_URL"
          echo "Resolving staging host (best-effort)..."
          STAGING_HOST=$(echo "$STAGING_URL" | sed -E 's#^https?://##' | cut -d/ -f1)
          getent hosts "$STAGING_HOST" || true
          
          MAX_ATTEMPTS=30
          ATTEMPT=0
          WAIT_TIME=10
          
          while [ $ATTEMPT -lt $MAX_ATTEMPTS ]; do
            ATTEMPT=$((ATTEMPT + 1))
            echo "Attempt $ATTEMPT/$MAX_ATTEMPTS: Checking staging health..."
            echo "Backend check: $STAGING_URL/api/health"
            BACKEND_STATUS=$(curl -s -o backend_body.txt -w "%{http_code}" -m 10 "$STAGING_URL/api/health" || echo "000")
            echo "Backend HTTP status: $BACKEND_STATUS"
            head -c 300 backend_body.txt 2>/dev/null || true
            echo ""
            
            # Check backend health
            if [ "$BACKEND_STATUS" = "200" ]; then
              echo "✅ Backend is ready!"
              
              # Check frontend health
              echo "Frontend check: $STAGING_URL/health"
              FRONTEND_HEALTH_STATUS=$(curl -s -o frontend_health_body.txt -w "%{http_code}" -m 10 "$STAGING_URL/health" || echo "000")
              echo "Frontend /health HTTP status: $FRONTEND_HEALTH_STATUS"
              head -c 300 frontend_health_body.txt 2>/dev/null || true
              echo ""

              echo "Frontend check: $STAGING_URL"
              FRONTEND_ROOT_STATUS=$(curl -s -o frontend_root_body.txt -w "%{http_code}" -m 10 "$STAGING_URL" || echo "000")
              echo "Frontend / HTTP status: $FRONTEND_ROOT_STATUS"
              head -c 300 frontend_root_body.txt 2>/dev/null || true
              echo ""

              if [ "$FRONTEND_HEALTH_STATUS" = "200" ] || [ "$FRONTEND_ROOT_STATUS" = "200" ]; then
                echo "✅ Frontend is ready!"
                echo "✅ Staging environment is fully deployed and ready for DAST scan"
                exit 0
              else
                echo "⏳ Frontend not ready yet, waiting..."
              fi
            else
              echo "⏳ Backend not ready yet, waiting..."
            fi
            
            if [ $ATTEMPT -lt $MAX_ATTEMPTS ]; then
              echo "Waiting ${WAIT_TIME} seconds before next attempt..."
              sleep $WAIT_TIME
            fi
          done
          
          echo "⚠️ Warning: Staging environment may not be fully ready, but proceeding with DAST scan..."
          echo "This could result in incomplete scan results."

  dast-scan-staging:
    name: DAST Scan (Staging)
    needs: deploy-staging
    runs-on: ubuntu-latest
    environment:
      name: staging
    if: github.ref == 'refs/heads/master' || github.ref == 'refs/heads/develop'
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      
      - name: Verify staging environment is accessible
        env:
          STAGING_URL: ${{ secrets.STAGING_ENVIRONMENT_URL || 'https://staging.drwheels.com' }}
        run: |
          echo "Verifying staging environment is accessible before DAST scan..."
          echo "Staging URL: $STAGING_URL"
          
          # Final health check before starting DAST
          if curl -f -s -m 10 "$STAGING_URL/api/health" > /dev/null 2>&1; then
            echo "✅ Backend is accessible"
          else
            echo "⚠️ Warning: Backend may not be fully ready"
          fi
          
          if curl -f -s -m 10 "$STAGING_URL" > /dev/null 2>&1; then
            echo "✅ Frontend is accessible"
          else
            echo "⚠️ Warning: Frontend may not be fully ready"
          fi
          
          echo "Proceeding with DAST scan..."
        continue-on-error: true

      - name: ZAP Baseline Scan (Staging)
        uses: zaproxy/action-baseline@v0.12.0
        env:
          STAGING_URL: ${{ secrets.STAGING_ENVIRONMENT_URL || 'https://staging.drwheels.com' }}
        with:
          target: ${{ env.STAGING_URL }}
          cmd_options: '-r zap-report.html -J zap-report.json -w zap-report.md'
        continue-on-error: true
      
      - name: Upload ZAP reports
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: zap-dast-staging-reports
          path: |
            zap-report.html
            zap-report.json
            zap-report.md
          retention-days: 30
      
      - name: Verify ZAP reports were generated
        run: |
          if [ -f zap-report.html ] || [ -f zap-report.json ] || [ -f zap-report.md ]; then
            echo "✅ ZAP reports generated"
          else
            echo "⚠️ ZAP reports were not generated (environment may have been unreachable)"
          fi
        continue-on-error: true

# ============================================
# STAGE 6: DEPLOY TO PRODUCTION
# ============================================
  deploy-production:
    name: Deploy to Production
    needs: [build-images, deploy-staging, dast-scan-staging]
    runs-on: ubuntu-latest
    environment:
      name: production
    if: github.ref == 'refs/heads/master'
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      
      - name: Deploy to Production
        run: |
          echo "Deploying to production environment..."
          echo "Backend Image: ${{ needs.build-images.outputs.backend-image }}"
          echo "Frontend Image: ${{ needs.build-images.outputs.frontend-image }}"
          # Add your deployment commands here
          # Example: kubectl set image deployment/backend backend=${{ needs.build-images.outputs.backend-image }}
      
      - name: Update GitOps Repository
        run: |
          git config --global user.name "GitHub Actions"
          git config --global user.email "actions@github.com"
          
          # Clone the GitOps repo
          git clone https://${{ secrets.WHEELOPS_TOKEN }}@github.com/mohamed99akram/wheelops.git
          cd wheelops
          
          # Update backend image tag
          sed -i "s|image: .*/backend:.*|image: ${{ needs.build-images.outputs.backend-image }}|g" drwheels/backend-deployment.yaml
          
          # Update frontend image tag
          sed -i "s|image: .*/frontend:.*|image: ${{ needs.build-images.outputs.frontend-image }}|g" drwheels/frontend-deployment.yaml
          
          # Commit and push changes
          git add .
          git commit -m "Update images to ${{ github.sha }}" || exit 0
          git push || exit 0
      
      - name: Production Health Check
        run: |
          echo "Performing production health check..."
          sleep 30
          # Add health check here
          # Example: curl -f ${{ secrets.PRODUCTION_ENVIRONMENT_URL }}/health || exit 1

# ============================================
# STAGE 7: POST-DEPLOYMENT MONITORING
# ============================================
  post-deployment-monitoring:
    name: Post-Deployment Monitoring
    needs: deploy-production
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/master'
    environment:
      name: production
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      
      - name: Run Smoke Tests
        run: |
          echo "Running smoke tests on production..."
          # Add smoke tests here
          # Example: npm run smoke-tests -- --url ${{ secrets.PRODUCTION_ENVIRONMENT_URL }}
      
      - name: Monitor Application
        run: |
          echo "Monitoring application health..."
          # Add monitoring checks here
